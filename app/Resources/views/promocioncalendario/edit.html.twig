{% extends '::layout_base.html.twig' %}
{% trans_default_domain "app" %}
    {% block content %}
        <div class="box box-default">
            <div class="box-header with-border">
                <h3 class="box-title">Editar Promoción Calendario</h3>

                <div class="box-tools pull-right">
                </div>
            </div>
            <!-- /.box-header -->
            <div class="box-body">

                <div class="row">
                    <div class="col-md-12">

                        {{ form_start(edit_form) }}
                        {{ form_widget(edit_form) }}
                        <button type="submit" class="btn btn-primary"><i
                                    class="fa fa-save"></i> {{ 'button.save'|trans }}
                        </button>
                        {{ form_end(edit_form) }}
                    </div>
                </div>
                <!-- /.row -->
            </div>
        </div>
        <div class="form-buttons">

            <a class="btn btn-default"
               href="{{ path('promocion_calendario_index') }}">{{ 'button.backtolist'|trans }} </a>

        </div>
    {% endblock %}
